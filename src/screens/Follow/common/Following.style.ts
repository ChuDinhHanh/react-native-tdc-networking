import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  tabBarLabelStyle: {
    fontWeight: 'bold',
    textTransform: 'none',
    fontSize: 16,
  },
});

export default styles;
