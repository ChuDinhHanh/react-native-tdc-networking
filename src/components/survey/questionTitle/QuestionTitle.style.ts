import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    body: {
        flexDirection: 'row',
        alignItems: 'center'
    },
    questionTitle: {
        flex: 1,
        color: '#000',
        fontSize: 16,
        fontWeight: 'bold',
    }
})

export default styles;