import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  group: {
    marginTop: 15,
    marginHorizontal: 5,
  },
  ip: {
    fontSize: 16,
    borderBottomWidth: 1,
    borderColor: '#97A1B0',
    padding: 5,
    marginBottom: 10,
  },
});

export default styles;
