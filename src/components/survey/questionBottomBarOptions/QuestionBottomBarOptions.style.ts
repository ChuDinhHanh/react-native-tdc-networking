import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  body: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  btnDelete: {
    marginStart: 'auto',
    marginEnd: 5,
  },
});

export default styles;
