import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  radioInputBody: {
    flexDirection: 'row',
    alignItems: 'center',
    color: '#000',
    marginTop: 5,
  },
  radioInputTitle: {
    fontSize: 16,
  },
});

export default styles;
